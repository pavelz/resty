%div[user]
  %p.name
    %strong Name
    = h user.name
  %p.email
    %strong Email
    = h user.email
  %p.password
    %strong Password
    = h user.password

  = link_to 'Show', object_path(user)
  |
  = link_to 'Edit', edit_object_path(user)
  |
  = link_to 'Destroy', object_path(user), :confirm => 'Really destroy user?', :method => :delete
